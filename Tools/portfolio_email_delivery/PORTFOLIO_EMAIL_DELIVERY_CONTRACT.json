{
  "schema": "portfolio-email-delivery-contract/1.2",
  "status": "CURRENT",
  "purpose": "Teacher-approved, privacy-preserving weekly email delivery of current Portfolio student reports from the canonical private Portfolio Drive. Contact import, report preparation, message-note preparation, and explicit per-send exclusions are Portfolio-state/delivery operations; actual email sending is always an explicit teacher action in a teacher-owned Google Apps Script web app.",
  "separation_rule": "This is a Portfolio delivery workflow only. It does not share state, logic, recipient data, or message-note state with the District Grading & Evidence tool.",
  "privacy_rules": [
    "Never write student names, student email addresses, guardian email addresses, student-specific teacher notes, report PDFs, or send logs to GitHub.",
    "Never put student identity, evidence, status, recipient data, or student-specific notes into a public URL or QR code.",
    "All contact sources, normalized contacts, recipient maps, report PDFs, teacher message notes, and send logs remain inside the canonical private Portfolio Drive root.",
    "Dashboard links may contain only a non-sensitive course hint. The server still resolves and validates the course against the private Portfolio root.",
    "The sender web app must be restricted to the teacher account and must fail closed when the active account cannot be verified."
  ],
  "drive_state": {
    "root_contact_directory": "00 Contact Directory",
    "root_contact_source_imports": "00 Contact Directory/Source Imports",
    "normalized_contact_directory": "00 Contact Directory/contact_directory.csv",
    "contact_import_history": "00 Contact Directory/contact_import_history.csv",
    "unit_recipient_file": "02 Portfolio Data/email_recipients_current.csv",
    "unit_contact_review_file": "02 Portfolio Data/email_contact_review.csv",
    "unit_delivery_manifest": "02 Portfolio Data/email_delivery_manifest.csv",
    "unit_message_notes": "02 Portfolio Data/email_message_notes.csv",
    "unit_send_log": "02 Portfolio Data/email_send_log.csv",
    "unit_delivery_pdf_folder": "03 Student Packets/Email Delivery"
  },
  "contact_import": {
    "accepted_source_types": [
      "xlsx",
      "csv"
    ],
    "source_classification": "setup/support file; never student evidence",
    "xlsx_required_headers": [
      "LastFirst",
      "Student Email",
      "Guardian Email"
    ],
    "xlsx_sheet_rule": "Read every worksheet containing all three required headers. Do not infer course period from worksheet name. The same student may legitimately appear on more than one worksheet.",
    "student_identity_rule": "Normalize duplicate source rows by exact school student email first. When the same student email appears on multiple sheets, duplicate identical contact data may be collapsed. Conflicting names or materially different guardian sets require review; do not silently choose or union conflicting records.",
    "guardian_parse_rule": "Split guardian addresses on comma or semicolon, trim whitespace, discard empty tokens, validate email syntax, and deduplicate case-insensitively while preserving the first trimmed spelling.",
    "registry_match_order": [
      "exact existing student email when the registry already contains one",
      "exact normalized first+last name within the current roster when unique",
      "teacher review"
    ],
    "forbidden_match_methods": [
      "persistent row-position matching",
      "fuzzy name guessing",
      "guessing from username patterns",
      "silently merging conflicting guardian sets"
    ],
    "raw_source_rule": "Preserve each imported contact source unchanged in 00 Contact Directory/Source Imports with source SHA-256 and import timestamp.",
    "setup_only_rule": "The teacher may run Portfolio contact setup without new student evidence. That operation updates only private contact-directory/import history and safely resolvable Unit recipient files; it does not change evidence ledgers/statuses/PowerSchool state or imply a new report run."
  },
  "contact_directory_columns": [
    "contact_key",
    "student_name_source",
    "student_email",
    "guardian_emails",
    "source_file_sha256",
    "source_sheets",
    "contact_status",
    "verified_at"
  ],
  "recipient_file_columns": [
    "student_key",
    "student_name",
    "period",
    "student_email",
    "guardian_emails",
    "contact_status",
    "contact_source_sha256",
    "verified_at"
  ],
  "guardian_storage_rule": "guardian_emails is a pipe-delimited list of already-trimmed, case-insensitively deduplicated addresses. A blank list is allowed only as a warning state, never as an invented address.",
  "recipient_statuses": {
    "READY": "Student email is valid and at least one valid guardian email is available.",
    "WARNING_NO_GUARDIAN": "Student email is valid but no guardian email is available. The sender may send to the student only after the teacher sees the warning.",
    "REVIEW": "Identity/contact conflict, invalid address, or unresolved roster match. Live class sending is blocked until resolved or that exact row is explicitly excluded from the current send."
  },
  "individual_report": {
    "rule": "Every successful Portfolio run creates the normal duplex-safe period packet AND one separate two-page PDF per rostered student from the same canonical student report rendering.",
    "folder": "03 Student Packets/Email Delivery",
    "identity_rule": "The manifest student_key and Drive file ID are authoritative. Never infer student identity from a filename.",
    "content_rule": "The individual PDF is exactly the student's same two report pages, with no extra cover page and no other student's content."
  },
  "delivery_manifest_columns": [
    "student_key",
    "student_name",
    "period",
    "course",
    "unit",
    "report_number",
    "report_date",
    "pdf_file_id",
    "pdf_file_name",
    "pdf_sha256"
  ],
  "message_notes": {
    "file": "02 Portfolio Data/email_message_notes.csv",
    "columns": [
      "scope",
      "student_key",
      "note",
      "include_this_week",
      "updated_at"
    ],
    "max_note_length": 1000,
    "weekly_rule": "The optional WEEKLY row stores a Unit-level teacher note used for the current weekly communication. Blank means no weekly note block is included.",
    "student_rule": "Optional STUDENT rows are keyed only by exact student_key. The note text persists privately; student names are not duplicated into this note file.",
    "preflight_custom_note_rule": "The Preflight table replaces the former Report column with an optional Custom note field. A nonblank value is saved privately for that exact current recipient and sets include_this_week true. Clearing the field turns inclusion off for the current message. The underlying report attachment is still verified server-side and is not editable from the table.",
    "include_rule": "A student note is inserted into an email only when include_this_week is true and the note text is nonblank. Successful LIVE delivery automatically resets include_this_week to false for that student while preserving the stored text. Failed/unsent students keep their current include state.",
    "preview_rule": "The preflight table must visibly show the weekly note and every active student-specific custom note. Send Test to Me must use the exact same note content that a live send would use.",
    "digest_rule": "The exact weekly note, active per-student notes, and included/excluded row scope are part of the preview digest. Editing notes or changing the include scope invalidates the old preview and requires a fresh preflight before test or live sending.",
    "privacy_rule": "Message notes are private Portfolio communication state, not curriculum content or student evidence. They never belong in GitHub, public dashboards, URLs, or QR codes."
  },
  "send_log_columns": [
    "send_id",
    "timestamp",
    "mode",
    "course",
    "unit",
    "report_number",
    "student_key",
    "report_sha256",
    "to_email",
    "bcc_emails",
    "status",
    "error"
  ],
  "sender": {
    "implementation": "Teacher-owned Google Apps Script web app",
    "project_files": [
      "Tools/portfolio_email_delivery/Code.gs",
      "Tools/portfolio_email_delivery/Index.html",
      "Tools/portfolio_email_delivery/appsscript.json"
    ],
    "required_script_properties": [
      "PORTFOLIO_ROOT_FOLDER_ID",
      "TEACHER_EMAIL",
      "TEACHER_DISPLAY_NAME"
    ],
    "authorization_rule": "The active Google account must exactly match TEACHER_EMAIL. Blank or different active-account identity fails closed.",
    "deployment_rule": "Deploy under the teacher's school Google Workspace account, execute as the teacher, and restrict access to the teacher/most restrictive school-admin-supported setting. Never deploy as public/anonymous.",
    "course_discovery_rule": "The sender discovers course folders and unit N folders from the canonical private Portfolio root. The same sender is used for Algebra 1, Physics, AP Calculus, STEM, and future courses once their private Portfolio course/unit state is initialized.",
    "dashboard_hint_rule": "A teacher dashboard may append ?course={URL_ENCODED_COURSE} to the web-app URL. The hint only preselects an existing private Portfolio course. If the hinted course is not initialized, the app must show that state and must not silently switch to a different course.",
    "preflight_rule": "Before live sending, join email_recipients_current.csv to email_delivery_manifest.csv by exact student_key; verify exactly one current report per included student; verify each included Drive file is inside the exact Email Delivery folder; recompute each included PDF SHA-256; validate recipients; display all warnings/errors, active message notes, and include checkboxes; compute a digest of the exact previewed send state. Only included rows contribute blockers, warnings, message counts, recipient totals, quota use, tests, or live sends.",
    "row_exclusion_rule": "Every Preflight row has a teacher-controlled include checkbox next to the student name. Unchecking it marks that row EXCLUDED for the current preview/send only. Exclusion may be used for former students or another intentional one-send omission. It never deletes or edits the report PDF, delivery manifest, recipient file, student registry, contact directory, evidence state, or send history. Exclusions are explicit session scope and are included in the preview digest.",
    "orphan_report_rule": "A report manifest row without a matching current recipient remains BLOCKED by default because the mismatch may be accidental. The teacher may explicitly uncheck that exact row to make it EXCLUDED for the current send; all other included rows can then proceed if Preflight passes.",
    "digest_rule": "Live send recomputes preflight after acquiring a script lock. If recipients, report hashes, report IDs, report numbers, selected period, weekly note, active student notes, or included/excluded rows changed, the preview digest no longer matches and sending is blocked until the teacher previews again.",
    "student_to_rule": "Place the student's valid school email in To.",
    "guardian_rule": "Place that student's deduplicated guardian emails in BCC. Never place guardian addresses in To or CC and never combine multiple students/families in one message.",
    "subject_pattern": "{COURSE} Unit {UNIT} Portfolio Progress Report - {STUDENT_NAME}",
    "body_rule": "Centralize the family-facing wording in buildBody_(). The standard message explains the approximately weekly report cadence, the body-of-evidence grading model, how Mastery Goals and I Cans organize the report, the current four-Mastery-Goal grade pilot when that course uses it, and the five fixed Next labels. The optional weekly note and optional active student note are inserted as clearly labeled blocks. Exact wording may be revised after teacher/family feedback without changing recipient or safety logic.",
    "next_key": [
      {
        "label": "Needs first check",
        "meaning": "We do not yet have usable evidence for this skill."
      },
      {
        "label": "Practice + check soon",
        "meaning": "More practice or support is needed before another independent check."
      },
      {
        "label": "Needs another independent check",
        "meaning": "There is already convincing evidence, but another independent demonstration is needed to confirm mastery."
      },
      {
        "label": "Secure",
        "meaning": "Current evidence supports mastery of this skill."
      },
      {
        "label": "Extension",
        "meaning": "The skill is secure and the student is ready to apply it in a new or more challenging situation."
      }
    ],
    "subject_privacy_rule": "Do not place grades, status codes, mastery labels, I Can results, guardian names, or message-note content in the subject line.",
    "test_send_rule": "Send Test to Me sends one selected included student's exact current report and exact current message-note content only to TEACHER_EMAIL with a [TEST] subject prefix. It never sends to the student or guardians and does not count as a live delivery.",
    "weekly_teacher_approval_rule": "Weekly is a preparation/communication cadence, not an unattended schedule. Portfolio may prepare fresh reports and preserve draft notes, but every live send still requires teacher review, a fresh passing preflight, optional teacher-only test, explicit confirmation, and a manual Send Reports action.",
    "live_send_rule": "Live sending requires a fresh passing preflight over the exact included rows and explicit teacher confirmation in the web app.",
    "quota_rule": "Count all To + BCC recipients for included sendable rows and compare with MailApp.getRemainingDailyQuota(). If remaining quota is insufficient for the entire currently included sendable selection, block before sending any live messages.",
    "duplicate_send_rule": "A successful LIVE log row for the same student_key + report_sha256 makes that exact report ALREADY_SENT and it is skipped on later runs. Failed rows may be retried after a fresh preflight.",
    "concurrency_rule": "Use a script lock around live preflight + send so double-clicks/concurrent sessions cannot duplicate a class send.",
    "failure_rule": "After a passing class preflight, log each individual send success/failure. Continue through the included prepared class if one MailApp call fails, then report the exact failure count. A later retry sends only unsent report hashes.",
    "blocking_conditions": [
      "unverified teacher account",
      "missing required private state file",
      "duplicate student_key in recipient or report manifest state on an included row",
      "recipient contact_status REVIEW on an included row",
      "missing or invalid student email on an included row",
      "invalid guardian email token on an included row",
      "missing report on an included current recipient row",
      "multiple reports for the same included student_key",
      "included orphan report without a matching current recipient row",
      "missing Email Delivery folder when an included report file must be verified",
      "included report file not inside the exact Email Delivery folder",
      "included report SHA-256 mismatch",
      "malformed or duplicate message-note rows",
      "preview digest mismatch, including note or include/exclude changes",
      "insufficient MailApp recipient quota for the included send"
    ],
    "warning_conditions": [
      "no guardian email; student-only delivery remains sendable after teacher review",
      "post-send student-note include reset failed after successful delivery; the send remains successful and the teacher is warned to clear the include flag manually"
    ]
  }
}
