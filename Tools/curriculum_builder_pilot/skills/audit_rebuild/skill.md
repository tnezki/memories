# Audit + Rebuild Bank Map Pilot Skill

Purpose: local current-shape-first maintenance wrapper.

Procedure:
1. Resolve the actual deployed `banks/unitN/source_map/` first.
2. Verify accepted map manifest/status and current authorities.
3. Create AI work order from current deployed map + exact current PM/Framework/profile only.
4. Preserve correct bytes; repair only concrete current-contract defects.
5. Different historical generation pins are not defects by themselves.
6. Future local runner validates changed-file boundary, hashes, finalization, and narrow transfer package.
7. Stop at good enough.

No Git commands. No public web. No File Library.
