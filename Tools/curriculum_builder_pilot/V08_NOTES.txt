CURRICULUM BUILDER v0.8
=======================

Normal Full Bank Pipeline:
  fresh Map -> local Map audit -> Complete Bank -> local Bank audit -> one final transfer ZIP

Manual AI bridge:
  ONE normal handoff ZIP -> ONE AI_FULL_BANK_RESULT.json

Local storage:
  ~/Documents/Curriculum Builder/
    Workspace/
    AI Exchange/
    Runtime/
    Legacy Archive/   (only when teacher clicks cleanup)

Downloads:
  no permanent pilot workspace
  AI result may arrive there from the browser, but Continue moves it into AI Exchange

Final delivery remains:
  ~/Downloads/_github_transfers/

Convenience launcher created automatically:
  ~/Applications/Curriculum Builder.command

No Git commands. No GitHub writes. No direct canonical repo writes.
