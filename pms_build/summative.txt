BUILD SUMMATIVE — SINGLE PASS / BANK-V2 SECURE BLUEPRINTS / PHYSICS SIX-FORM ASSESSMENT SHELL LOCK
============================================================================================================

STATUS: CURRENT
STEP: 11
JOB: summative
ROLE: Build the complete current Unit Summative assessment from the exact Unit Assessment Plan + COMPLETE Bank-v2 secure Summative blueprints and render it in the current deployed Physics Summative shell.

This is ONE build PM.
There is no nested Summative PM folder, local PM manifest, control-panel profile, starter ZIP, bundled CSS, or legacy archive dependency.

FLOW:
  PHILOSOPHY + RUNTIME + QUESTION STRUCTURE + PHYSICS FRAMEWORK + ORIGINAL UNIT ASSESSMENT PLAN + COMPLETE BANK-V2
    -> LOAD EXACT SUMMATIVE BLUEPRINT A / B FAMILY RECORDS
    -> INSTANTIATE V1-V6 SECURE FORMS
    -> VALIDATE I-CAN / EVIDENCE / SECURITY / REPRESENTATIONS
    -> SCRAMBLE FINAL SELECTED-RESPONSE ANSWER POSITIONS ON STAGED FORM DATA
    -> RENDER SIX FORMS + BUBBLE ANSWER SHEET + FR RESPONSE SHEET
    -> SEMANTIC / SECURITY / KEY / PRINT-CONTRACT / FINAL-BYTE QA
    -> PACKAGE ONE UNIT SUMMATIVE ZIP

======================================================================
0. TOOL POLICY — EXECUTABLE HARD GATE
======================================================================

web_search_allowed = false
web_fetch_allowed = false
external_research_allowed = false
repository_search_for_declared_sources_allowed = false

This is a NO-WEB curriculum build.

Resolve declared authorities through manifests/contracts and DIRECT-FETCH exact paths at pinned snapshots.
Pin the canonical system repository and selected course repository once at job start.
Never mix later commits into the run.

If a declared required source cannot be fetched:
  FAIL CLOSED
and report the exact repository/path.
Do not search around the failure.
Do not substitute a stale upload, prior ZIP, chat copy, remembered version, similarly named file, or older finished Summative.

======================================================================
1. AUTHORITY / READ ORDER
======================================================================

MANDATORY:
1. current canonical `Curriculum_Philosophy.txt`;
2. current canonical `SYSTEM_RUNTIME_CONTRACT.txt`;
3. current canonical `_question_structure/` entrypoint plus only structure contracts actually used by the selected Summative families;
4. current canonical Physics Framework front door through the Framework manifest;
5. ORIGINAL requested Unit Assessment Plan resolved by that Framework;
6. current Framework-declared Physics Bank-v2 course profile;
7. expanded current Unit Bank front door + item index;
8. exact Summative Blueprint A and Blueprint B routes declared by the Unit Bank manifest;
9. this PM;
10. current deployed course `css/summative_physics.css`;
11. canonical registered tools only when this PM or the selected family route requires them.

Authority by domain:
1. current explicit teacher instruction;
2. Curriculum Philosophy;
3. Question Structure for question/evidence structure;
4. Physics Framework + ORIGINAL Unit Assessment Plan for exact Unit scope, I-cans, evidence architecture, and course behavior;
5. current Bank-v2 profile + COMPLETE Unit Bank for secure Summative family records;
6. this PM for assembly procedure;
7. current deployed Summative CSS / shell implementation for rendering mechanics.

The current or older finished Summative HTML may be inspected only as a structure/path sanity check when needed.
It is NOT a question source and must not be used to manufacture secure variants.

======================================================================
2. REQUIRED INPUTS / COMPLETE BANK-V2 HARD GATE
======================================================================

Resolve the requested Unit from the current request/orchestrator.

Require the expanded current Unit Bank to be:
- `status = COMPLETE`;
- `ready_for_downstream = true`;
- the requested Unit;
- compatible with the current Framework-declared Physics Bank-v2 profile.

Resolve all Bank paths through `BANK_MANIFEST.json` / `ITEM_INDEX.json`.
Do not search for prior Bank ZIPs or stage handoffs.

For current Physics Bank-v2 Summative architecture require:
- `record_mode = family_seed`;
- 40 Summative family seeds per Unit;
- Blueprint A = 20 family records;
- Blueprint B = 20 family records;
- form size = 20;
- selected response = 16 per form;
- constructed response = 4 per form;
- forms = V1, V2, V3, V4, V5, V6.

Every consumed family record must provide an executable complete secure family contract, including as applicable:
- family / record ID;
- primary exact I-can ID/text;
- evidence job;
- intended reasoning;
- question structure;
- response mode/class;
- representation mode/need;
- approximate difficulty intent;
- complete canonical seed;
- complete selected-response choices/key when applicable;
- answer / solution / scoring basis;
- executable representation route;
- locked elements;
- allowed variations;
- required variations;
- forbidden variations;
- security/freshness rules;
- provenance.

If either blueprint, a family transformation contract, a representation route, answer/scoring basis, or required metadata is incomplete:
  FAIL CLOSED as SUMMATIVE_BANK_FAMILY_INCOMPLETE.

Do not backfill from exposed Notes, Practice, Warm-Up, CYU, Exit, Review, or a prior finished Summative.

======================================================================
3. BLUEPRINT / FORM ARCHITECTURE — HARD
======================================================================

Use the EXACT upstream-mapped family records.
Do not reselect or substitute families downstream merely because another family appears easier to instantiate.

CURRENT PHYSICS CONTRACT:

BLUEPRINT A:
- exact 20 mapped families;
- V1, V2, V3, V4 are controlled parallel secure instances of those same 20 evidence families.

BLUEPRINT B:
- exact 20 mapped families;
- V5, V6 are controlled secure instances of those same Blueprint B families;
- Blueprint B is the materially alternate / makeup architecture.

For EVERY form:
- exactly 20 questions;
- exactly questions 1-16 selected response;
- exactly questions 17-20 constructed response;
- every exact Unit I-can must be directly sampled at least once;
- remaining slots intentionally weight high-leverage / compound mastery evidence.

Blueprint A and B may overlap where the upstream Bank intentionally maps the same high-leverage learning, but Blueprint B must remain materially alternate overall in evidence families, contexts, representations, and/or dependency paths.
Do not force artificial disjointness that weakens the mapped evidence.

The Bank/Assessment Plan owns the evidence blueprint.
This renderer does not create a second blueprint.

======================================================================
4. SECURE INSTANCE GENERATION — HARD
======================================================================

Each final form question is a validated secure instance of its exact mapped family.

LOCKED across same-family secure instances:
- primary I-can;
- evidence job;
- question structure;
- response demand;
- intended reasoning;
- approximate difficulty;
- required representation behavior;
- any other family-declared locked element.

Secure variation must change at least one strong evidence-primary axis allowed by the family, such as:
- evidence/givens set;
- physical context when context is evidence-bearing;
- representation values/type/orientation when allowed;
- misconception/error target when allowed;
- dependency path;
- values/units when compatible with the evidence job.

NOT sufficient as security by itself:
- noun/name swap;
- number-only swap;
- sentence-order change;
- wording-only paraphrase;
- answer-choice reordering;
- version/question-number tokens.

HARD:
- no exposed instructional finished prompt reuse;
- no prior finished Summative prompt reuse as a shortcut;
- no sibling-form prompt that is only cosmetically changed;
- no change to I-can/evidence job/difficulty merely to make a variant easier to write.

For each instance complete the actual student-facing:
- prompt;
- choices when selected response;
- correct answer / scoring basis;
- solution or validation reasoning;
- required table/graph/diagram/model/response surface.

If a secure instance cannot be produced inside the family contract:
  REBUILD THAT INSTANCE
or fail closed if the family lacks an executable route.
Do not relabel metadata to make drift appear valid.

======================================================================
5. SELECTED RESPONSE — 16 PER FORM / FOUR CHOICES
======================================================================

For every selected-response item:
- render exactly four complete choices unless the current family explicitly and validly declares another count; current Physics Summative requires four;
- exactly one keyed answer;
- all distractors distinct and plausible;
- distractors arise from the exact misconception/error space of the item;
- no throwaway, joke, duplicate, or generic distractor bag;
- choice content must remain synchronized with the final representation and values.

Answer-position balancing is NOT secure variation.

After ALL SIX FORMS' content, values, representations, choices, and keys are FINAL:

1. create staged instantiated Summative form data;
2. run canonical:
     `Tools/scramble_selected_response.py`
   on the staged instantiated form data, NOT on canonical Bank family seeds;
3. use strict current Physics mode:
     `--strict-16x4`
4. require for each form:
   - exactly 16 recognized selected-response items;
   - exactly four choices each;
   - final correct-answer distribution A/B/C/D = 4/4/4/4;
   - no invalid key/choice synchronization;
5. render the final HTML from the scrambled staged form data;
6. independently re-check every rendered selected-response answer against the rendered choice order.

The canonical Bank remains unchanged.
Never mutate the Bank just to balance display positions.

If the strict tool sees Summative records with choices but recognizes zero selected-response items:
  FAIL CLOSED.
Do not accept a no-op.

======================================================================
6. CONSTRUCTED RESPONSE — QUESTIONS 17-20
======================================================================

Each form contains exactly four constructed-response prompts numbered 17-20.

Each must:
- execute its mapped evidence job;
- collect evidence appropriate to its mapped I-can;
- preserve the family's intended reasoning and difficulty;
- supply every required representation or response scaffold;
- remain answer-neutral;
- have a complete internal scoring/solution basis used for QA.

The student artifact directs full work to the separate constructed-response answer sheet.

When the student must construct a diagram/model:
- provide only the blank/object/scenario response surface required by the task;
- do not show the completed answer representation.

Do not embed teacher answer keys or scoring guidance in the student-visible artifact.

======================================================================
7. REPRESENTATIONS / ASSETS / TOOL ROUTING
======================================================================

Representation metadata is executable.

For small data tables:
- use real semantic HTML tables;
- preserve usable headings, values, units, and captions when required.

For graphs / deterministic diagrams:
- use the Framework/Bank-declared registered tool route when one is required;
- preserve the registered visual contract;
- verify scale, labels, units, physical meaning, and answer-neutrality after generation.

For canonical course image assets already deployed under a stable course-relative path:
- reference the exact existing deployed course path from the final Summative HTML;
- verify that exact declared path exists at the pinned course snapshot;
- DO NOT copy, download/reconstruct, base64-decode, checksum-recover, re-encode, or package duplicate image bytes merely for the Summative ZIP.

For generated inline SVG allowed by the selected family/tool route:
- it may remain inline in the Summative HTML;
- it must be generated from the current executable representation contract, not hand-drawn from memory;
- verify the final values/labels exactly match the prompt/key.

HARD:
- never replace a required information-bearing representation with prose;
- never use a decorative substitute;
- never expose answer information through a completed response surface;
- every final representation must match the final secure instance after all variation.

If a required representation cannot be executed:
  FAIL CLOSED
unless the exact I-can/family explicitly permits an evidence-equivalent redesign within its allowed variation contract.

======================================================================
8. OUTPUT PATH / PHYSICS SUMMATIVE SHELL — HARD
======================================================================

Primary live course path:

  assessments_zxrtjp/unit{U}_summative/unit{U}_summative_assessment.html

For Unit 1:

  assessments_zxrtjp/unit1_summative/unit1_summative_assessment.html

Use the current deployed Physics Summative shell/CSS.
Do not rebuild a competing stylesheet.

HEAD:
- exactly one:
    `<link rel="stylesheet" href="../../css/summative_physics.css">`
- do NOT add `base.css`;
- MathJax configuration must appear before the MathJax loader;
- preserve inline `$...$` and display `$$...$$` processing.

The deployed `summative_physics.css` is a standalone black-and-white exam stylesheet.

BODY CONTRACT:
- one optional `.no-print` navigation block for teacher/browser navigation;
- exactly six `.assessment-form` sections with:
    `data-form="V1"` through `data-form="V6"`;
- form order V1, V2, V3, V4, V5, V6.

For EACH form:

SELECTED RESPONSE:
  `<section class="assessment-flow mc-flow force-page-break-after">`
- form title + Name line + visible Form label;
- directions;
- exactly 16 `.q` selected-response items numbered 1-16;
- each selected response visibly renders all four choices.

CONSTRUCTED RESPONSE:
  `<section class="assessment-flow fr-flow force-page-break-after">`
- visible Form label;
- constructed-response directions;
- exactly four `.fr` prompts numbered 17-20.

PHYSICAL PAGE FLOW:
- assessment content flows naturally across physical sheets;
- use exactly two intentional logical breaks per form:
  1. after selected response 1-16;
  2. after constructed response 17-20;
- DO NOT wrap arbitrary question ranges in fixed `.page` containers;
- DO NOT force a question-count-per-sheet target;
- let the current CSS/print engine flow content naturally while preventing unacceptable clipping/splits.

AFTER ALL SIX FORMS:

1. exactly one bubble answer sheet:
   - class includes `.page.answersheet`;
   - Name line;
   - Version bubbles V1, V2, V3, V4, V5, V6;
   - selected-response rows 1-16;
   - A/B/C/D bubble choices for each row.

2. exactly one constructed-response answer sheet:
   - `.fr-sheet-page`;
   - 2 x 2 response table/areas;
   - numbered 17, 18, 19, 20;
   - clear instruction to show work / identify final answers as appropriate.

No answer key appears in the student artifact.

Do not modify `css/summative_physics.css` to compensate for malformed artifact HTML.
Fix the artifact.

======================================================================
9. HIDDEN TRACEABILITY
======================================================================

Student-visible output must NOT show:
- Bank IDs;
- family IDs;
- I-can IDs;
- security labels;
- DOK/Bloom;
- question_structure_id;
- transformation notes;
- answer keys;
- internal scoring metadata.

Hidden `data-*` attributes may preserve deterministic audit lineage, for example:
- `data-source-family-id`;
- `data-primary-ican-id`;
- `data-blueprint`;
- `data-form`;
- `data-evidence-slot`.

Do NOT use `data-bank-id` to imply that a downstream secure instance is an exact canonical Bank prompt.
The family seed is provenance; the finished secure variant is a downstream instance.

======================================================================
10. INDEPENDENT VALIDATION — ALL 120 FINAL QUESTIONS
======================================================================

After final wording, data, choices, representations, and scrambling are complete, independently solve/check EVERY rendered question:

6 forms x 20 questions = 120 final instances.

For each final item verify:
- prompt is coherent and complete;
- all necessary information is supplied;
- no irrelevant filler values;
- keyed answer / scoring basis is correct;
- selected-response choices are distinct and plausible;
- units, signs, magnitudes, directions, and physical relationships are correct;
- representation matches final prompt/values;
- representation is answer-neutral;
- exact mapped I-can/evidence job is actually tested;
- response mode can collect the promised evidence;
- approximate difficulty remains inside family intent;
- security/freshness passes against exposed instructional content;
- security/freshness passes against sibling secure forms.

FORM / BLUEPRINT QA:
- V1-V4 use exact Blueprint A family lineage;
- V5-V6 use exact Blueprint B family lineage;
- every form has 16 SR + 4 CR;
- every form directly samples every exact Unit I-can;
- V1-V4 are genuine secure siblings, not cosmetic clones;
- V5-V6 are genuine secure siblings of Blueprint B;
- Blueprint B remains materially alternate overall from Blueprint A;
- no exposed instructional finished prompt is reused.

If any instance fails:
  repair/rebuild that instance
  and rerun all dependent answer/representation/security checks.

======================================================================
11. RENDER / PRINT-CONTRACT / FINAL-BYTE QA
======================================================================

SOURCE / ROUTING:
- pinned system + course snapshots used consistently;
- Philosophy, Runtime, Question Structure, Framework, original Assessment Plan, Bank profile, Bank front door, and both Summative blueprints resolved in order;
- no prior Summative ZIP or finished test used as curriculum authority.

CONTENT:
- exactly six forms V1-V6;
- exactly 20 questions per form;
- exactly 16 SR + 4 CR per form;
- exact Blueprint A/B lineage preserved;
- all Unit I-cans covered per form;
- all 120 final questions independently validated.

SELECTED RESPONSE:
- complete visible four-choice sets;
- strict scrambler PASS;
- A/B/C/D distribution 4/4/4/4 per form;
- rendered choice order and key synchronized after scrambling.

SECURITY:
- no exposed instructional prompt reuse;
- no superficial-only secure variation;
- no hidden answer leakage;
- Blueprint B materially alternate overall.

REPRESENTATIONS:
- every required table/graph/diagram/model present;
- all final values/labels agree with prompt/key;
- no completed answer shown where the student must construct it;
- all referenced canonical course assets resolve;
- no duplicate canonical image bytes packaged inside the Summative folder.

HEAD / MATHJAX / CSS:
- exactly one `../../css/summative_physics.css`;
- no `base.css`;
- MathJax config before loader;
- no malformed math delimiters / raw unresolved LaTeX;
- shared Summative CSS unchanged.

DOM:
- six `.assessment-form` sections;
- per form one SR flow + one CR flow;
- 16 `.q` + 4 `.fr`;
- one answer sheet after forms;
- one FR response sheet after answer sheet;
- no duplicate IDs;
- no unresolved template tokens.

PRINT / VISUAL — DETERMINISTIC / NON-BLOCKING:
- validate print/layout behavior from the FINAL HTML plus the current deployed `css/summative_physics.css`;
- verify the DOM/CSS print contract directly: question blocks are not malformed, required flow/break classes are present, form breaks occur only after Q16 and Q20, the bubble sheet and FR sheet have their required classes/structure, and `.no-print` content is governed by the deployed print CSS;
- verify figures/tables/SVG/MathJax containers use the deployed shell conventions and do not contain obvious fixed dimensions or malformed markup that would force clipping;
- DO NOT generate a PDF solely for Summative QA;
- DO NOT invoke a PDF skill, browser-render pipeline, headless browser, screenshot pass, or print-to-PDF pass solely to prove layout;
- DO NOT create a modified QA HTML copy with MathJax/CDN scripts removed, replaced, or rewritten;
- DO NOT require successful network access to the external MathJax CDN during local QA; source ordering/configuration correctness is the required check;
- DO NOT create or package QA HTML, PDF, screenshots, browser traces, or other secondary QA deliverables;
- if a local visual renderer is already available and completes without external fetches or material delay, one OPTIONAL spot-check may be used to catch obvious clipping, but unavailable/failed visualization tooling is NOT a build failure when deterministic HTML/CSS QA passes;
- if an optional visual spot-check reveals a real artifact defect, repair the FINAL HTML and rerun deterministic dependent checks; do not build a parallel QA artifact.

DELIVERY BOUND:
- once content/key/security checks, strict scrambling, deterministic DOM/CSS print-contract checks, final-byte checks, and ZIP-boundary checks pass, PACKAGE AND DELIVER immediately;
- do not continue into secondary rendering, PDF creation, browser experimentation, CDN workarounds, or extra QA packaging.

FINAL BYTES:
- reopen the exact final HTML bytes after QA;
- all local/course-relative references resolve from the deployed path;
- no debug text, QA report, staging metadata, or hidden keys leaked into student-facing HTML.

Do not invent new QA criteria mid-run.
Park new systemic findings for later PM revision.

======================================================================
12. OUTPUT / ZIP BOUNDARY — CURRENT PHYSICS
======================================================================

Current Physics Summative delivery is ONE ZIP containing the complete Unit Summative folder.

ZIP filename:

  unit{U}_summative.zip

ZIP ROOT — EXACT:

  unit{U}_summative/
    unit{U}_summative_assessment.html

For Unit 1:

  unit1_summative.zip
    unit1_summative/
      unit1_summative_assessment.html

The ZIP is intended to be extracted/copied into the EXISTING course:

  assessments_zxrtjp/

Therefore the deployed Unit 1 path becomes:

  assessments_zxrtjp/unit1_summative/unit1_summative_assessment.html

HARD PACKAGING RULES:
- no outer `physics/` folder;
- no outer `assessments_zxrtjp/` folder;
- first ZIP folder is exactly `unit{U}_summative/`;
- include the complete final Summative HTML;
- do not include unchanged shared CSS;
- do not include duplicate canonical course images referenced in place;
- do not include PM, Framework, Assessment Plan, Bank, Question Structure, tools, QA logs, staged JSON, internal answer keys, or scratch files.

Internal answer/scoring data is used for validation but is not packaged unless current explicit teacher instruction or the current Framework separately requires a teacher-facing key artifact.

FINAL ZIP QA:
- archive opens successfully;
- top-level folder exactly `unit{U}_summative/`;
- expected Summative HTML exists exactly once;
- no extra wrapper folders;
- delivered HTML bytes match the QA'd final HTML bytes.

END — BUILD SUMMATIVE
