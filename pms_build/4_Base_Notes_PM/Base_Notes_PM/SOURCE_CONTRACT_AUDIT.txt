BASE NOTES PM STARTER v4.8 — SOURCE CONTRACT AUDIT
==================================================

PASS — Normal upload set is exactly Starter ZIP + current Framework ZIP + finalized Unit Bank ZIP.
PASS — Curriculum Philosophy v3.3 is bundled; no separate Philosophy upload is required.
PASS — Universal/course-specific layering is explicit.
PASS — No Spiral/AP/Science routing branch is required in the Base Notes PM.
PASS — Course-specific Notes behavior is delegated to the current Course Framework.
PASS — Finalized Unit Bank remains canonical for WTC / Notes Example / Notes YTI content.
PASS — Every substantial reading requires an immediate content-specific Stop and Discuss.
PASS — S&D is NOT automatically attached to every Example/YTI.
PASS — Useful Math/Model Notes abundance is allowed; no automatic one-per-pair template.
PASS — First meaningful vocabulary/notation introduction may be bolded; recurrence clutter is prohibited.
PASS — Visible Example/YTI numbering follows Notes teaching order while canonical IDs remain metadata.
PASS — Production HTML retains exactly base.css then notes.css.
PASS — Framework-approved layout/class extensions are allowed without creating an unapproved third stylesheet.
PASS — Canonical graphs must use the Framework graph class rather than bare <figure> markup.
PASS — Simple two-column task tables use the Framework compact 50/50 table structure when specified.
PASS — Three-column vocabulary sort markup matches deployed Notes CSS: table=`vocab-sort-table`, blank row=`tr.vocab-sort-space`, directions wrapped by `vocab-sort-task` / `vocab-sort-directions`.
PASS — Three-row skim / "Let's Get Started" markup matches deployed Notes CSS: directions=`skim-section-directions`, table=`skim-section-table`; no legacy `preview-notes-table` fallback.
PASS — Descriptive captions/titles beneath student-facing representations are prohibited; mathematically necessary labels remain inside the representation.
PASS — Exact typography/graph/table/workspace values are delegated to the current Framework visual contract; stale older numeric defaults are prohibited.
PASS — Summary workspace is not a universal requirement; closing workspace behavior comes from the Framework.
PASS — Build means complete student + teacher Notes package, not scaffold/checkpoint.
PASS — Cross-document conflict audit is required before delivery.

Normal pilot upload set:
  4_Base_Notes_PM.zip
  current Course Framework ZIP
  finalized Unit Bank ZIP

Prompt:
  Carefully read PM and Run.

PASS — Pre-ZIP staging HTML is repaired with Framework ~fix_mathjax_v10.py before final ZIP creation.
