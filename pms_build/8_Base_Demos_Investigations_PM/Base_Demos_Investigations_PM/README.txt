8_Base_Demos_Investigations_PM.zip

Fresh-chat inputs:
1. This Starter ZIP
2. Current course Framework ZIP

Typical prompt:
Carefully read PM and Run.

A Bank is optional secondary reference, not a required input.

Course-wide builds use a hard PLAN-FIRST gate: create the complete section-by-section Investigation/Demo map before any HTML, QA that map, then render from it. The final ZIP retains ~demos_investigations_build_plan.json + .html so the teacher can inspect/alter the optional design map. Use the current Framework graph/figure tool when a supported visual/model genuinely serves the section.

Current starter reconciliation: Philosophy v3.3 is bundled. Before the final ZIP, run the current Framework `tools/~fix_mathjax_v10.py` against the complete staging output and QA the repaired bytes.

GENERIC RUN
Use exactly: `Carefully read PM and Run.`
The current PM package determines the operation; do not paste an older resource-specific prompt.
