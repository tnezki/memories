BUILD NOTES — STEP 4B / BANK-V2
=================================

STATUS: CURRENT
ROLE: Build complete student + teacher Notes from the accepted current 4A map and finalized Bank-v2 Notes records.

4B is ADAPT / ASSEMBLE / RENDER.
It is NOT a second mapping stage.
It is NOT a Bank audit.
It is NOT permission to redesign Notes HTML/CSS.

REQUIRED MAP
------------
  notes/uN_notes_map/uN_notes_map.json

Require:
  schema = physics-notes-map/2.0
  notes_flow_revision = bank-v2-textbook-chunks-v2

Do not reinterpret an older Notes map.
Older revision -> FAIL CLOSED and rerun 4A.

STARTUP / AUTHORITY
-------------------
Read current Philosophy + Runtime.
Resolve Framework/Notes profile/visual contract and declared sources directly through pinned manifests.
Pin memories/course/textbook commits once.
No web/File-Library/prior-ZIP/repository hunting for declared sources.

BANK IDENTITY
-------------
Read current:
  banks/unitN/BANK_MANIFEST.json

Require COMPLETE + ready_for_downstream.
For every map-declared Notes section:
- resolve canonical route;
- fetch exact file;
- compare current blob identity to map-recorded blob identity.

Changed consumed Notes file -> rerun 4A.
Do not whole-Bank fingerprint or scan unrelated destinations.

FORMAT IS LOCKED
----------------
Use the established course Notes shell/CSS.

For current Physics:
  ../../css/base.css
  ../../css/notes.css

Exactly in that order.

Do NOT:
- invent a new stylesheet;
- add a third ordinary Notes stylesheet;
- redesign typography/colors/cards/tables/page architecture;
- invent a new class system where the established shell already supports the job.

SOURCE-SPINE / CLOSE READING — HARD
-----------------------------------
The main instructional reading comes from the map-declared retained textbook/source spans.

For CLOSE:
- preserve source paragraph logic/useful wording;
- trim mainly by omission.

For LIGHT_CONDENSE:
- preserve source explanation/order;
- trim repetition/detail;
- light paraphrase allowed.

For STRONG_CONDENSE:
- preserve essential conceptual job only when 4A explicitly mapped it.

Do NOT invent a replacement close-reading passage merely to connect tasks.
The normal operation is select/cut/lightly adapt the declared source.

OPENING — HARD
--------------
Render:
1. title / exact LT / exact I-cans;
2. Vocabulary Preview;
3. canonical WTC;
4. Let's Get Started skim;
5. ONE Close Reading directions block;
6. first reading chunk.

Skim wording:
  "Skim before close reading. Look at the headings and figures. Find 1–2 things you notice and 1–2 things you wonder about."

Do not add a prediction row unless current Framework explicitly requires it.
Render the Close Reading directions box exactly once.

READING RHYTHM — HARD
---------------------
Walk map reading chunks in source order.

At each reading boundary render at most ONE primary student-processing event:
- Stop & Discuss
- one canonical Example/YTI pair
- none

Never render Stop & Discuss AND Example/YTI at the same reading boundary.

Mapped Physics Notes/diagrams/graphs/models may be embedded as instructional support with the reading.
They do not count as a competing primary student-processing event.

EXAMPLE / YTI DISPLAY NUMBERING
-------------------------------
Preserve hidden Bank IDs, pair_id, pair_number, canonical prompt/solution.

Visible labels use `display_pair_number` from 4A:
- Example 1 / You Try It 1
- Example 2 / You Try It 2
- ...

Do NOT expose Bank pair_number as the visible number when display order differs.

After the final reading chunk, render any `post_reading_ex_yti_queue` pairs in order with continued display numbering.

CANONICAL BANK CONTENT
----------------------
WTC / Example / YTI student tasks and Bank-owned representations remain exact.
Do not rewrite/swap/split a canonical Bank task in ordinary Notes production.

If a Bank defect is discovered, report it upstream; do not silently create a second canonical question in Notes.

TABLES / GRAPHS / BANK REPRESENTATIONS
--------------------------------------
Preserve canonical Bank student_html/representation behavior.

Small canonical data tables:
- semantic HTML;
- `<table class="values">`;
- compact/centered through existing Base CSS;
- no pipe-delimited text.

Graphs:
- use canonical Bank graph asset/output;
- do not redraw/restyle in Notes.

Source/toolkit images:
- reference Physics Notes Image Toolkit assets in place;
- do not copy toolkit binaries into section folders.

Generated Notes-specific visual:
- only if explicitly mapped by 4A;
- only when canonical/source/toolkit content does not already satisfy the need;
- student-facing result remains answer-neutral.

CLOSE — HARD
------------
Render exactly:
1. Vocabulary with definitions;
2. bulleted Summary;
3. What I Figured Out;
4. Common Mistakes;
5. final miscellaneous workspace only when current Framework/shell requires it.

What I Figured Out must not disappear.
Do not add a Notes Exit Check.

STUDENT PAGINATION — NOTABILITY-FIRST
-------------------------------------
Use existing CSS-supported keep-together wrappers for atomic instructional units.

Keep together when practical:
- complete WTC unit;
- skim task;
- compact reading heading + reasonable chunk + associated figure;
- Physics Note;
- Stop & Discuss;
- each Example;
- each YTI;
- figure + caption/labels;
- Common Mistakes block.

Do NOT wrap a giant multi-page reading in one avoid-break block.

Forced page breaks:
- NOT for ordinary crowding/layout repair;
- only for an intentionally separate page/workspace explicitly required by current Framework/shell.

Let natural pagination move intact blocks.

TEACHER COPY — ONLINE-FIRST
---------------------------
Derive from finalized student structure.

Teacher copy:
- same student content/order;
- release fixed student workspace heights;
- vertical space is not a concern;
- answers/support push later content downward;
- overlap is a hard failure.

MULTIPART ANSWERS — HARD
------------------------
Never dump a multipart canonical solution into one giant red paragraph.

Format teacher support as readable structure, e.g.:

Answer / Acceptable Reasoning
- (a) ...
- (b) ...
- (c) ...

Teacher Moves
- ...
- ...

Student Moves
- ...
- ...

Listen For / Likely Misconception
- ...

Use lists whenever there is more than one part/idea.
Answers remain clearly teacher-only/red according to established course behavior.

TOOLS
-----
Resolve through Tools/MANIFEST.json.
Run `fix_notes_layout_contract` as currently declared when required.
Do not hunt for undeclared finalizers.

TARGETED QA
-----------
Per section verify:
- exact targets;
- accepted v2 map/revision;
- correct Bank Notes blob identities;
- WTC once;
- skim wording correct;
- Close Reading directions exactly once;
- main reading is retained/adapted declared source, not invented generic replacement prose;
- no reading boundary contains both S&D and EX/YTI;
- EX/YTI visible numbering sequential by display order;
- queued pairs after reading present;
- mapped Physics Notes/diagrams rendered appropriately;
- semantic compact tables preserved;
- no student answer leakage;
- What I Figured Out appears after Summary;
- student Notability wrapping/page flow works;
- no ordinary forced page breaks;
- teacher answers/support use lists and expand naturally;
- no overlap;
- established base.css -> notes.css order;
- no invented Notes visual system.

OUTPUT
------
Return ONE ZIP:
  unitN_notes.zip

ZIP root:
  uN_1_notes/
    uN_1_notes.html
    uN_1_notes_teacher.html
    figures/ only when referenced generated local figures exist
  ...
  _notes_provenance/
    unitN/
      uN_notes_map.json
      NOTES_MAP_HANDOFF.json
      NOTES_BUILD_HANDOFF.json
      NOTES_BUILD_REPORT.txt
      IMAGE_USE_MANIFEST.json

Do not include toolkit binaries, PM files, or staging files.
