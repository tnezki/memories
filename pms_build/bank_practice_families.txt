BUILD BANK PRACTICE FAMILIES — STEP 3B / BANK-V2
==================================================

STATUS: CURRENT
ROLE: Author the complete canonical Practice family layer into the existing modular Unit Bank.

STARTUP / INPUT
---------------
Read current Philosophy, Runtime, Question Structure, Framework/Assessment Plan, Bank-v2 profile, Tools manifest, and:
  banks/unitN/BANK_MANIFEST.json
  banks/unitN/ITEM_INDEX.json
  banks/unitN_bank_map/MAP_MANIFEST.json
  banks/unitN_bank_map/unitN_question_design_map.json
  banks/unitN_bank_map/practice_map.json

Require 3A complete, Bank BUILDING, same accepted map/design identity, and 3B permitted.
No prior ZIP discovery.

PROFILE-REVISION CONTINUATION — HARD
------------------------------------
An already accepted Bank Map / completed prior stage may continue under a newer current Bank-v2 profile ONLY when the current profile explicitly declares backward compatibility for the stage being entered.

For Physics profile v1.3:
- v1.3 changes WTC purpose/acceptance semantics and WTC metadata verification only;
- Practice, Exit, and Summative contracts are unchanged from v1.2;
- an accepted v1.2 map plus completed v1.2 3A may continue into 3B/3C without rerunning completed stages when the accepted map fingerprint/design identity is unchanged;
- record both the accepted-map profile identity and the current profile identity in provenance.

If a profile revision changes any contract or slice consumed by the stage being entered, or if explicit compatibility is absent, FAIL CLOSED rather than assuming compatibility.

PURPOSE
-------
Author every mapped Practice family seed.
Do NOT materialize Practice Set 1/2/3/Extra as canonical Bank inventory.

FAMILY CONTRACT
---------------
Each family contains:
- family_id exactly equal mapped ID
- record_mode = family_seed
- destination = practice
- section
- family_type
- exact primary/supporting I-cans
- evidence_job
- intended_reasoning
- question_structure_id
- response_mode
- representation_mode / representation_need
- question_design object with design_slot_id + tool/render route
- difficulty_intent
- security_level
- selection_tags
- fully authored canonical_seed:
  - student_text / student_html
  - choices when applicable
  - answer
  - solution_text / solution_html
  - figure_refs
- explicit locked/allowed/required/forbidden variation rules
- representation variation rule
- source_map_record_id / source_basis

A seed is a complete classroom-usable question, not a template.

QUESTION STRUCTURE / RENDER EXECUTION — HARD
--------------------------------------------
Every canonical seed must execute its mapped structure, response mode, representation, and tool route.

Selected response:
- complete visible choices;
- keyed correct answer;
- plausible task-specific distractors.

Small data table:
- semantic HTML table;
- `<table class="values">`;
- no pipe-delimited pseudo-table.

Graph:
- resolve/use `graph_tool` when supported;
- inspect actual render.

Diagram/FBD/vector:
- approved source/deterministic route only;
- no crude or physically wrong improvised visual;
- missing approved route -> fail that item.

Representation-required seeds must visibly contain the representation in student_html.

PHYSICS QUALITY
---------------
- concrete physical situations for quantitative work;
- relevant data only;
- physically plausible states/units;
- equations as relationships;
- real representation use, not decorative metadata;
- meaningful semantic variation where required;
- no noun-swap pseudo-variety.

FAMILY FIDELITY
---------------
A downstream sibling preserves:
- exact I-can/evidence job;
- question structure;
- intended reasoning;
- comparable difficulty;
- required representation behavior;
- locked elements.

It satisfies required variation using only allowed axes.
Surface-only name/object changes do not count when deeper variation is required.

STORAGE
-------
Create/update:
  banks/unitN/BANK_MANIFEST.json
  banks/unitN/ITEM_INDEX.json
  banks/unitN/practice/section_<section-id>.json
  banks/unitN/figures/... only when needed
  banks/unitN/provenance/3b_build.json
  banks/unitN/provenance/3b_report.txt

Preserve all 3A canonical content unchanged.

QA — HARD
---------
Verify:
- every mapped Practice family authored exactly once;
- each design_slot resolves;
- pool counts/mixes match map/profile;
- seed executes mapped structure/response/representation/tool;
- selected-response choices complete/visible;
- small tables semantic/compact;
- graph_tool used when supported;
- required diagrams/visuals valid and readable;
- every seed independently solved against final representation;
- locks/variation rules resolvable;
- required variation cannot be satisfied by prohibited surface-only change;
- all figure refs resolve;
- ITEM_INDEX/BANK_MANIFEST correct.

Render-level failure = Bank failure.

FULL-SNAPSHOT DELIVERY
----------------------
Return ONE ZIP:
  unitN.zip

Complete current Unit Bank after 3B.
Preserve 3A files.
Do not include banks/, unitN_bank_map/, PM files, or prior ZIPs.
