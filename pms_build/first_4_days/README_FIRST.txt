BUILD FIRST 4 DAYS PM v1.5

Use this universal PM with the CURRENT self-contained course Framework plus the REQUIRED course-specific opening plan/source package. Do not run with zero opening-plan inputs unless the Framework itself explicitly contains an authoritative First 4 Days plan.
Core rule: build a private exact-I-can JSON map / temporary micro-bank BEFORE authoring questions or HTML. Do not deploy that map as a Unit 0 Bank.

Normal output is one course-root-relative patch under misc/first_4_days/ with a top-level index.html.

Reference family CSS/HTML inside this PM is reference-only. Live course CSS remains authoritative.

WTC default: embedded in Notes + Notes teacher guide. Activity subfamily CSS routing and canonical q/solution classes are hard contracts. The Activity Options hub also carries the complete projectable directions/expectations pages for each listed structure; it is not just a link menu. Known-good Unit references are structure-only, not content authority.

Return complete deployable resource-family folders when a family is being replaced; a true one-file repair may be returned alone with its exact repository-relative destination path.

CONTROL PANEL INPUT NOTE: The preferred controller path is a structured opening-plan form that creates FIRST_4_DAYS_OPENING_PLAN.json; source/assets remain optional supplements.
