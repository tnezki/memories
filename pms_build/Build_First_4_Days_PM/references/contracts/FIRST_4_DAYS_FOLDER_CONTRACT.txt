FIRST 4 DAYS — CANONICAL DEPLOYMENT SHAPE

misc/first_4_days/
  index.html
  first_4_days.css
  figures/
  warmups/first_4_days_warmups.html
  notes/
  # WTC lives inside notes/ by default; add wtc/ only when explicitly requested standalone.
  practice/
  activities/0_1_act1/        normal Activity family when requested
  investigation/
  exit/
  overview/                    when requested
  survey/                      when requested

This is a structural reference, not a content quota. Build only requested resources.
