BASE RECURRING + ASSESSMENT RESOURCES PM STARTER v2.0
=====================================================

Use this ONE Starter for Warm-Ups, Exit Tickets, Progress Trackers, Reviews, and Summative Assessments.

EMBEDDED MODE IN PM 10 (NORMAL USE):
This family PM is already inside `10_Base_Pract_to_Sum_PM.zip`. Do NOT ask the teacher to upload PM 7 separately.
The outer PM + current Framework + finalized Unit Bank are the complete normal input set.

Standalone debug mode only:
If this family PM is deliberately extracted and run by itself, use the current Framework ZIP + finalized Unit Bank ZIP.

Do not upload a separate Curriculum Philosophy; it is bundled here.
Do not upload separate CSS; the approved CSS references are bundled here.
Do not upload the Assessment Plan separately when the current Framework embeds it.

The Framework owns course-specific counts, routing, paths, names, and assessment family.
This Base PM owns universal extraction/rendering/package mechanics.

The css/ folder is a reference/install-once library. Ordinary Unit resource ZIPs link to the deployed course-root CSS and do not duplicate CSS.


v1.2 WARM-UP CORRECTION
------------------------
Locked the tested Notability Warm-Up HTML: simple Section/Day header only; no kicker/subtitle/name/date; no visible workspace element. Blank card area is the writing space.




v1.4 PHYSICS SUMMATIVE TAIL + ZIP DELIVERY CORRECTION
------------------------------------------------------
Locked the approved final-two-page Physics layout: `.page.answersheet` followed by the current `.fr-sheet-page` / `.fr-sheet-table` 2x2 work sheet for 17–20. Legacy `.frgrid` remains CSS compatibility only and is prohibited in new builds. Added source-vs-output representation preservation checks and made a real ZIP archive the required final deliverable.

v1.3 SUMMATIVE STALE-TEMPLATE + PHYSICS BUBBLE CORRECTION
---------------------------------------------------------
Current Framework + finalized Bank own the active Summative form IDs. Old rendered HTML can never add forms; answer-sheet selectors are generated from the active IDs. Physics bubble sheets use separate CSS circle elements so letters stay normal-sized while bubbles remain easy to mark.


v1.6 CANONICAL HTML SHELL UPDATE
- Added tested Warm-Up, Exit Ticket, Progress Tracker, Review, Review Practice references.
- Added secure-content-free Algebra/Calculus Summative DOM shell.
- Bundled fallback Philosophy is synchronized to v3.12; current root Philosophy wins. Pre-ZIP MathJax/final-byte gate uses the current v11-or-newer contract.

v1.9 COMBINED-BUILD / WORKSPACE UPDATE
- Preserves q-compact/q-standard/q-large/q-xlarge shell classes.
- Adds per-question workspace sizing; do not default unresolved items to q-standard.
- Adds flexible current-item-count Summative shell (including tested 3+3+2 for 8-item forms).
- Replaces hard-coded Unit 1 Progress reference with a generic Mad-Lib tracker shell.
- Keeps image/table shell enforcement and deterministic checks.

v2.0 FINAL-BYTE / TRACKER / REVIEW / SUMMATIVE REFINEMENT
- Hard-fails malformed single \x / \y / \z system row breaks and requires post-ZIP verification.
- Progress Tracker accepts only exact source statements explicitly designated as I-cans.
- Review visible title names the true review-target Unit, with routed/current Unit only as context.
- Summative sizing uses deterministic response-footprint signals and protects obvious compact/large cases from blanket q-standard defaults.
- Embedded fallback Philosophy is synchronized to v3.12; current root Philosophy wins when packaged.

GENERIC RUN
Use exactly: `Carefully read PM and Run.`
The current PM package determines the operation; do not paste an older resource-specific prompt.


v2.6 EXIT UPDATE
- Projector Ticket A/B/C remains four-up exact Bank Version 1.
- Collected form provides four normalized versions per ticket: one exact Anchor, two tight parallels, one controlled variation, balanced across positions.
- Two portrait pages/version, two problems/page, with response accessories when needed.
- Systemic PM bottlenecks are proposed for teacher approval and rerun/audited; they do not silently rewrite authorities.


v2.9 SHARED CSS + SELECTED-RESPONSE SCRAMBLE
- Base mechanics remain universal across Algebra, Calculus, and Physics; the Framework selects the Summative profile.
- Course-root CSS is shared; `summative_math.css` uses `base.css`, while `summative_physics.css` is standalone.
- `tools/~scramble_selected_response.py` deterministically reorders selected-response choices on a staging copy only.
- Current Physics 16x4 forms must finish exactly 4 A / 4 B / 4 C / 4 D before rendering/packaging.

v2.9 DETECTOR FIX: selected-response detection accepts current canonical `question_design.response_mode = selected_response` and strict Physics mode refuses false PASS_NOOP when Summative choice records are present.

V2.9: Universal Exit print JS, exact two-page physical Progress Tracker, and explicit Physics 16-row answer-sheet materialization are hard contracts.
