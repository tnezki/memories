BUILD BANK ASSESSMENT FAMILIES - PORTABLE MEMORY v1.0 - STEP 3C BANK-V2
======================================================================

STATUS: CURRENT BANK-V2 PILOT PM
ROLE: Author the complete secure Exit and Summative family layer and finish the canonical modular Bank-v2 build.

STARTUP - HARD
--------------
Read the current Curriculum Philosophy and Runtime Contract.
Resolve required authorities through the pinned manifests.
Fetch declared resources directly.


TOOL ROUTING GATE — HARD
------------------------
For this Bank job:
- `web_search_allowed = false`;
- `web_fetch_allowed = false`;
- `external_research_allowed = false`;
- repository search is forbidden for any authority/resource whose repo/path is already declared.

Do NOT invoke a browser/web-search tool or public-web fetch for source discovery, source verification, freshness checking,
examples, question writing, supplementation, or replacement curriculum content. Resolve exact paths through manifests and
fetch them directly from the pinned GitHub/course snapshot. User-supplied current attachments may also be read when the
request explicitly makes them inputs.

If a required source cannot be obtained through the declared direct route, STOP and report the exact missing repo/path or
manifest entry. Do not search around the failure. Outside research requires an explicit teacher request in the current job;
Framework/profile wording alone does not authorize it.

GENERIC RUN COMMAND
-------------------
Carefully read PM and Run.

"Run" means complete this entire Unit phase, author every mapped Exit and Summative family, validate security and family
contracts, update the real modular Bank, mark the Bank ready only if all phases pass, package only new/changed repo-relative
files, reopen the package, and return it in the same response.

PURPOSE
-------
Step 3C authors the canonical secure family seeds declared by:
  `exit_map.json`
  `summative_map.json`

It does NOT materialize finished Exit A/B/C forms or Summative V1-V6 forms. Those are downstream controlled secure instances
of the canonical families and blueprints stored here.

Bank-v2 sequence:
  2.5 Bank Map
    -> 3A Instructional Core
    -> 3B Practice Families
    -> 3C Assessment Families
    -> complete canonical modular Bank

Step 2.6 is NOT required.
There is no Stage 3D or Stage 3E in Bank-v2.

SCOPE
-----
Build one Unit at a time.

Author ONLY every `family_seed` record declared in:
- `banks/unitN_bank_map/exit_map.json`;
- `banks/unitN_bank_map/summative_map.json`.

Preserve all existing 3A/3B canonical files unchanged except:
- `banks/unitN/BANK_MANIFEST.json`;
- `banks/unitN/ITEM_INDEX.json`;
which 3C must update to finish routing/state.

Do NOT author or modify exposed instructional exact content or Practice family seeds.
Do NOT generate final Exit Ticket or Summative documents in this phase.

The course Bank profile owns counts/blueprints. Do not hard-code Physics inventory into another course.
For the current Physics Unit 1 pilot, the accepted map declares:
- Exit: 8 secure families per section x 5 sections = 40 families;
- Summative: 40 secure families per Unit = Blueprint A 20 + Blueprint B 20;
- total new 3C families = 80.

AUTHORITY ORDER
---------------
1. Current explicit teacher instruction for this job.
2. Current Curriculum Philosophy.
3. Current System Runtime Contract.
4. Current course Framework + ORIGINAL Unit Assessment Plan.
5. Current Framework/course manifest-declared Bank profile and required course runtime files.
6. Accepted Unit Bank-v2 map (`banks/unitN_bank_map/`).
7. Current `_question_structure/` authority.
8. Existing canonical Bank front door and authored 3A/3B files.
9. This PM for secure authoring and storage procedure.

PINNING / DIRECT FETCH - HARD
-----------------------------
At job start, pin the memories repository commit and course repository commit once. Do not mix later commits into the run.

Read the Bank front door first:
  `banks/unitN/BANK_MANIFEST.json`
  `banks/unitN/ITEM_INDEX.json`

Resolve the same accepted Bank Map through:
  `banks/unitN_bank_map/MAP_MANIFEST.json`

Fetch only the two map slices needed for 3C:
  `exit_map.json`
  `summative_map.json`

Use ITEM_INDEX to fetch the smallest existing 3A/3B canonical section files needed for security comparison. Do not rebuild a
giant Bank aggregate and do not repository-search for exposed prompts.

3A + 3B / MAP HARD GATE
-----------------------
Before authoring, verify:
- requested course and Unit are unambiguous;
- Bank status is BUILDING and `ready_for_downstream = false`;
- completed phases contain `3A_instructional_core` and `3B_practice_families`;
- next/pending phase permits `3C_assessment_families`;
- Bank manifest points to the same accepted Bank Map identity/fingerprint used by 3A/3B;
- Exit and Summative slice paths/fingerprints/counts agree with MAP_MANIFEST;
- every consumed record uses `record_mode = family_seed` and the correct destination;
- all family IDs are unique within the whole Bank index;
- exact I-can IDs/text resolve against the pinned Framework / ORIGINAL Assessment Plan;
- every question structure resolves through current `_question_structure/` or a declared course extension;
- Exit deterministic selection rules are feasible;
- Summative blueprint assignments, family counts, response-class counts, and I-can coverage are internally feasible.

If a required authority/family/blueprint is materially missing or contradictory, STOP. Do not rebuild the Bank Map inside 3C.

MAP CONTRACT NORMALIZATION - HARD
---------------------------------
Materialize a fully explicit canonical family contract. Do not copy unresolved semantic lock tokens forward.

Known pilot aliases may be normalized only when unambiguous:
- `question_structure` -> `question_structure_id`;
- `approximate_difficulty` -> `difficulty_intent`;
- `representation_job` -> explicit representation contract from `representation_mode`, `representation_need`, and any
  `representation_variation_rule`;
- `intended_reasoning` -> explicit field derived narrowly from mapped evidence job + question structure;
- `blueprint_assignment` -> explicit Summative blueprint field/tag from the accepted map;
- `response_class` -> explicit mapped selection tag / response-class field.

Every token in locked/allowed/required/forbidden rules must resolve to an explicit canonical property or transformation rule.
Unknown/unresolvable tokens are a hard stop. Record alias normalization in `provenance/3c_build.json`; do not mutate the map.

SECURITY AWARENESS - HARD
-------------------------
Before authoring secure seed prose, inspect the relevant exposed instructional content through ITEM_INDEX:
- 3A exact items for the same I-can/evidence neighborhood;
- 3B Practice canonical seeds for the same I-can/evidence neighborhood.

A secure family may target the same I-can, structure, formula, representation type, and reasoning architecture students have
practiced. It must not reuse an exposed finished prompt, distinctive data set, distinctive diagram, or distinctive stimulus.

Security does NOT mean forced novelty. It means fresh evidence while preserving the intended evidence job and difficulty.

COURSE-AWARE AUTHORING - HARD
-----------------------------
This PM is universal. The current course Framework/profile owns disciplinary behavior.

For Physics:
- quantitative assessment prompts begin with concrete physical situations, not naked variable assignments;
- equations are relationships describing phenomena;
- use physically plausible measurements/units;
- representations must actually carry the evidence when mapped;
- keep wording concise and assessment-appropriate.

For other courses, use their declared course patterns instead of importing Physics behavior.

CANONICAL SECURE FAMILY CONTRACT
--------------------------------
Each Exit/Summative map record becomes ONE fully authored secure canonical family record containing:

IDENTITY / ROUTING
- `family_id` exactly equal to mapped ID;
- `record_mode = family_seed`;
- `destination` = exit or summative;
- section when mapped;
- family type;
- exact primary/supporting I-can IDs/text;
- selection tags;
- explicit blueprint assignment/position for Summative when mapped.

EVIDENCE DESIGN
- evidence job;
- explicit intended reasoning;
- question structure ID;
- response mode / response class;
- representation mode/need;
- difficulty intent;
- security level;
- wording profile when mapped.

FULLY AUTHORED SECURE CANONICAL SEED
- `canonical_seed.student_text`;
- `canonical_seed.student_html`;
- `canonical_seed.choices` when applicable;
- `canonical_seed.answer`;
- `canonical_seed.solution_text`;
- `canonical_seed.solution_html`;
- `canonical_seed.figure_refs` for actual secure seed figures;
- actual embedded table/data/model content when part of the item.

TRANSFORMATION / SECURITY CONTRACT
- locked elements as explicit key/value locks;
- allowed variations as explicit permitted axes/rules;
- required variations as explicit changes required for each secure sibling;
- forbidden variations as explicit prohibited changes;
- representation variation rule when mapped;
- explicit security contract preserving freshness requirements;
- source map record ID and useful source basis.

Do NOT add audit workflow/status fields.
Do not expose family IDs, blueprint labels, security metadata, or map language to students.

SEED QUALITY - HARD
-------------------
Every secure family seed is a complete, valid assessment question with answer and full solution, not a prose template.
The seed demonstrates the exact evidence architecture that downstream secure instances must preserve.

Independently solve/check each seed after final wording, choices, data, and representations are fixed.
For selected response, exactly one listed answer must be defensible unless the declared structure explicitly requires otherwise.

EXIT FAMILY CONTRACT
--------------------
Author the full mapped Exit family pool, not only the current four-family feasibility witness.

For the Physics pilot:
- 8 canonical secure families per section;
- downstream ticket size = 4;
- A/B/C use the same deterministically selected four family IDs for a given build key;
- each A/B/C form uses a fresh secure instance of those families;
- exposed prompt reuse is forbidden;
- surface-only variation is forbidden;
- hard evidence/composition/security constraints are applied before deterministic tie-break.

3C stores the 8-family pool plus the accepted selection rules/metadata needed downstream. It does NOT author A/B/C forms.

SUMMATIVE FAMILY / BLUEPRINT CONTRACT
-------------------------------------
Author every mapped Summative family exactly once.

For the current Physics pilot:
- Blueprint A = 20 mapped family IDs used for V1-V4 secure instances;
- Blueprint B = 20 mapped family IDs used for V5-V6 secure instances;
- each blueprint owns its mapped response-class composition and I-can coverage;
- Blueprint B is materially alternate where the map requires it;
- answer-position scrambling happens downstream only after content is fixed and independently verified.

Do not invent a third blueprint, reshuffle mapped family membership, or materialize V1-V6 forms in the Bank.

SECURE VARIATION QUALITY - HARD
-------------------------------
A valid secure sibling preserves all locked evidence architecture while making the required fresh semantic change.

Invalid secure variation includes, when forbidden by the family/map:
- exposed finished prompt reuse;
- other secure form/blueprint finished-prompt reuse;
- names/objects only;
- wording profile only;
- choice order only;
- version/question number only;
- values/units only when a deeper fresh stimulus is required.

Valid variation may change mapped axes such as context/evidence, givens/evidence set, misconception target, representation
values/type, values/units, unknown quantity, orientation/geometry, dependency path, or wording profile when permitted.

REPRESENTATIONS / FIGURES
-------------------------
If a secure seed requires a graph, diagram, FBD, vector, table, model, or other representation, the actual student-safe
representation must exist and must not reveal the answer.

Purpose-built Bank figures belong under:
  `banks/unitN/figures/`

Use stable filenames tied to family IDs. Every figure reference must resolve. No orphan new figures.
Do not reuse an exposed instructional figure/data set merely to save work when security requires fresh evidence.

MODULAR CANONICAL STORAGE - HARD
--------------------------------
Do NOT create a giant Bank JSON and do NOT duplicate 3A/3B item bodies.

3C creates/updates only:
  `banks/unitN/BANK_MANIFEST.json`
  `banks/unitN/ITEM_INDEX.json`
  `banks/unitN/exit/section_<section-id>.json`
  `banks/unitN/summative/blueprint_a.json`
  `banks/unitN/summative/blueprint_b.json`
  `banks/unitN/figures/...` only for new secure seed figures actually needed
  `banks/unitN/provenance/3c_build.json`
  `banks/unitN/provenance/3c_report.txt`

Each Exit section file contains only that section's 8 canonical Exit families in deterministic family order.
Each Summative blueprint file contains only its mapped 20 canonical families in blueprint position order.

ITEM_INDEX UPDATE
-----------------
Append compact routes for all 3C families:
- family ID;
- record mode;
- destination;
- section or blueprint as applicable;
- exact canonical file path.

Do not add prompt/solution bodies to ITEM_INDEX.
Preserve every existing 3A/3B route exactly.

BANK_MANIFEST FINALIZATION
--------------------------
Only after every 3C hard gate passes:
- completed phases = 3A + 3B + `3C_assessment_families`;
- pending phases = empty;
- next phase = null / none;
- add Exit and Summative canonical artifact paths;
- update all destination/family/exact-content counts and total;
- preserve accepted Bank-map/profile identity;
- set Bank version to complete/current Bank-v2 form;
- set `status = COMPLETE`;
- set `ready_for_downstream = true`.

If any 3C QA gate fails, do NOT mark the Bank complete/ready.

AUTHORING ORDER
---------------
1. Read Philosophy + Runtime Contract.
2. Pin memories/course commits.
3. Resolve Framework, ORIGINAL Assessment Plan, Bank profile, and `_question_structure/` directly.
4. Read BANK_MANIFEST + ITEM_INDEX and verify 3A/3B state.
5. Read accepted MAP_MANIFEST, then only Exit + Summative map slices.
6. Reconcile Exit pools/selection constraints and Summative blueprints/coverage/response classes.
7. Resolve map contract aliases explicitly.
8. Use ITEM_INDEX to inspect relevant exposed 3A/3B content for security awareness.
9. Author all Exit secure canonical family seeds section by section.
10. Independently solve/check every Exit seed.
11. Author all Summative Blueprint A families in mapped order.
12. Author all Summative Blueprint B families in mapped order.
13. Independently solve/check every Summative seed.
14. Validate every transformation/security contract and required fresh-variation rule.
15. Generate/verify every required secure representation.
16. Write modular Exit/Summative files.
17. Append 3C routes to ITEM_INDEX and finalize BANK_MANIFEST only if all QA passes.
18. Write compact 3C provenance/report.
19. Run complete Bank-v2 QA.
20. Package only new/changed repo-relative `banks/unitN/` files and verify the package.

3C / COMPLETE BANK QA - HARD GATES
----------------------------------
Before returning PASS/ready:
- no browser/web-search or public-web fetch was invoked during this Bank job;
- no repository search was used to locate a source with an already-declared path;
- every mapped Exit family is authored exactly once;
- every mapped Summative family is authored exactly once;
- no 3A/3B canonical content is altered;
- Exit pool counts, selection constraints, and I-can/evidence coverage match the accepted map;
- Summative Blueprint A/B family IDs, order, response classes, and coverage match the accepted map;
- every secure seed is complete and independently solved;
- no secure seed simply reproduces an exposed instructional finished prompt/stimulus/data/figure;
- every required semantic freshness rule can actually be satisfied by the explicit allowed-variation contract;
- surface-only changes cannot satisfy required secure variation;
- every lock/allowed/required/forbidden token resolves explicitly; no unknown lock token survives;
- representation-bearing seeds include/respect their actual representations;
- Physics quantitative seeds use concrete physically plausible situations rather than naked variable assignments;
- every answer/solution matches the final prompt/choices/data;
- all new figure refs resolve, figures are student-safe, and no orphan new figures exist;
- no placeholders or audit-status fields exist;
- every ITEM_INDEX route resolves to the correct small canonical file and all prior routes remain unchanged;
- BANK_MANIFEST counts equal actual canonical inventory;
- completed phases contain exactly the required Bank-v2 authoring phases and pending phases are empty;
- `status = COMPLETE` and `ready_for_downstream = true` only after all gates pass;
- output package contains only new/changed repo-relative Bank files and no copy of the Bank Map.

DELIVERY CONTRACT
-----------------
Return one ZIP named:
  `<course>_uN_bank_3c_assessment_families.zip`

The ZIP preserves course-repository-relative paths beginning with:
  `banks/unitN/`

Teacher repository action:
Extract/merge the ZIP into the ROOT of the current course repository. Replace only supplied files. Do not replace the whole
`banks/` folder and do not retain the handoff ZIP as canonical source after extraction/sync.

FINAL RESULT
------------
After successful 3C, `banks/unitN/` is the canonical complete Bank-v2 source. Downstream Notes, Practice, Exit, Summative,
Activities, and repair workflows fetch only the smallest declared Bank files needed through BANK_MANIFEST + ITEM_INDEX.
